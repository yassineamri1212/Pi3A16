{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>Start_location</th>
                <td>{{ livraison.startLocation }}</td>
            </tr>
            <tr>
                <th>Delivery_location</th>
                <td>{{ livraison.deliveryLocation }}</td>
            </tr>
            <tr>
                <th>Is_delivered</th>
                <td>{{ livraison.isDelivered ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livraison_index') }}">back to list</a>

    <a href="{{ path('app_livraison_edit', {'id': livraison.id}) }}">edit</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
