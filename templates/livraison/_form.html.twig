<div class="form-wrapper">
    {{ form_start(form) }}
        {{ form_widget(form, {'attr': {'class': 'form-fields'}}) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>
