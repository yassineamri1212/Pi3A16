{# File: templates/admin/moyen_de_transport/edit.html.twig #}
            {% extends 'admin/base.html.twig' %}

            {% block title %}Edit Transport{% endblock %}

            {% block admin_content %}
            <div class="container-fluid">
                <div class="card shadow">
                    <div class="card-header bg-white">
                        <h1 class="h3 mb-0">Edit Transport</h1>
                    </div>
                    <div class="card-body">
                        {{ form_start(form) }}
                        {{ include('admin/moyen_de_transport/_form.html.twig', { 'button_label': 'Update' }) }}
                        {{ form_end(form) }}
                    </div>
                    <div class="card-footer bg-white d-flex justify-content-between align-items-center">
                        <a href="{{ path('admin_moyen_de_transport_index') }}" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-1"></i> Back to list
                        </a>
                        {{ include('admin/moyen_de_transport/_delete_form.html.twig', { 'transport': moyen_de_transport }) }}
                    </div>
                </div>
            </div>
            {% endblock %}